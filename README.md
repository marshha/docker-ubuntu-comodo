# docker-ubuntu-comodo
Build Docker container for Ubuntu running Comodo AV

## Build the container
Run the following commands

    cd docker
    sudo docker build -t ubuntu-comodo:$(date +%Y%m%d) .
    
